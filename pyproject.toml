[project]
name = "pdu-web-control"
version = "0.1.0"
description = "Flask web app to control a PDU via Arduino Uno (StandardFirmata) pin 13"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "flask-login>=0.6.3",
    "flask-sqlalchemy>=3.1.1",
    "pyfirmata2>=2.5.1",
]
